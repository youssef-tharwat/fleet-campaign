interface:
  display_name: "Fleet Campaign"
  short_description: "Run Herdr fleets with mandatory lane reviews"
  default_prompt: "Use $fleet-campaign to run this Herdr-managed engineering campaign with frozen gates, owned lanes, and a Code Review Pass before each lane completes."
